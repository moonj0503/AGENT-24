# Member 4 — Agents

Own `agents/**`, `tools/**`, and `policy/**`. Consume only `@continuity/contracts`; never call HTTP routes from an agent. The Policy Engine remains deterministic, and only the tool registry owner changes `tools/registry.ts`.
