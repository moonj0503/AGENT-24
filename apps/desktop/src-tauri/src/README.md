# Member 1 — Native & Privacy

Own `src-tauri/**`. Process raw window titles, blocked apps, and idle-time data locally. Send only sanitized `ActivityEvent` data through the observation boundary; do not call OpenAI directly.
