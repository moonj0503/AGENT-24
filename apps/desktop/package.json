{
  "name": "@continuity/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": { "build": "tsc -p tsconfig.json", "dev": "tsx src/main.ts", "test": "vitest run --passWithNoTests", "typecheck": "tsc --noEmit" },
  "dependencies": { "@continuity/contracts": "workspace:*" },
  "devDependencies": { "tsx": "^4.0.0", "vitest": "^4.0.0" }
}
