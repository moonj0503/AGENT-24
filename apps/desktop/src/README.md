# Member 2 — Product & Frontend

Own `src/**`. Build against `src/mocks` and `@continuity/contracts`; place every API adapter in `features/*/api.ts`. Do not import the API database or agent internals.
